apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-go
  namespace: ai-platform
  labels:
    app: api-go
  annotations:
    checkov.io/skip1: "CKV_K8S_43=Using image tags for dev environment"
    checkov.io/skip2: "CKV2_K8S_6=NetworkPolicy is handled globally by Kyverno"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-go
  template:
    metadata:
      labels:
        app: api-go
      annotations:
            checkov.io/skip1: "CKV_K8S_43=Using image tags for dev environment instead of sha256 digests"
            checkov.io/skip2: "CKV2_K8S_6=NetworkPolicy is handled globally by Kyverno/Cilium"
    spec:
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api-go
          image: ghcr.io/kirill140992/ai-inference-platform-api-go:v0.6.1
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: PORT
              value: "8000"
            - name: QDRANT_URL
              value: "http://qdrant:6333"
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"