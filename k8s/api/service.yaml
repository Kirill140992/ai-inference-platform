apiVersion: v1
kind: Service
metadata:
  name: api-go
  namespace: ai-platform
  labels:
    app: api-go
spec:
  selector:
    app: api-go
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP